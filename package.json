{
  "name": "meridian",
  "private": true,
  "workspaces": [
    "backend",
    "app",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -At run build",
    "test": "yarn workspaces foreach -At run test",
    "lint": "yarn workspaces foreach -At run lint",
    "abi:sync": "node scripts/sync-contracts.mjs"
  },
  "resolutions": {
    "vite": "^7.0.0"
  },
  "packageManager": "yarn@4.16.0"
}
